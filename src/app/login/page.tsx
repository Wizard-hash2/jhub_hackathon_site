import { PagePlaceholder } from "@/components/page-placeholder";

export default function LoginPage() {
  return (
    <PagePlaceholder
      title="Login"
      description="Authentication screens haven't been designed yet — send the Login screenshot and it will be built here."
    />
  );
}
