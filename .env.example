# PostgreSQL connection string used by the app (src/db/index.ts).
# If you change these credentials, also update drizzle.config.json.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db

# Password required to sign in at /adminlogin (change this!)
ADMIN_PASSWORD=change-this-password
